PORT=5000

# PostgreSQL bağlantı bilgileri (kendi bilgisayarındaki PostgreSQL'e göre değiştir)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kongre_yonetim
DB_USER=postgres
DB_PASSWORD=sifren

# JWT için gizli anahtar (rastgele uzun bir string yaz)
JWT_SECRET=cok-gizli-bir-anahtar-buraya-yaz
